function addSlideAnimationClass(element, animationClass) {
    element.classList.add(animationClass);
}

function removeSlideAnimationClass(element) {
    element.classList.remove("slide-enter");
    element.classList.remove("slide-exit");
}
